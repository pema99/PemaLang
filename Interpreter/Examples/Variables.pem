func main()
{
	'a variable declaration, variables can only be declared once
	var foo = "bar"
	println foo

	'a variable (re)assignment, assignments can happen infinite times
	foo = "foo"
	println foo
}